import {SearchTeacherPipe} from './search-teacher.pipe';

describe('SearchTeacherPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTeacherPipe();
    expect(pipe).toBeTruthy();
  });
});
