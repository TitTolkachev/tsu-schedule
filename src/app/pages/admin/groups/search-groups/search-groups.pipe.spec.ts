import {SearchGroupsPipe} from './search-groups.pipe';

describe('SearchGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
