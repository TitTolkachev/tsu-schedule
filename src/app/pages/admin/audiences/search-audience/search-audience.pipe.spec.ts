import {SearchAudiencePipe} from './search-audience.pipe';

describe('SearchAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
