import {SearchSubjectsPipe} from './search-subjects.pipe';

describe('SearchSubjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSubjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
